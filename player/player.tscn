[gd_scene load_steps=4 format=3 uid="uid://ci4bjp5e2bu3p"]

[ext_resource type="ArrayMesh" uid="uid://dc6ty4t0wyttc" path="res://player/possum.obj" id="1_oul6g"]

[sub_resource type="GDScript" id="GDScript_oul6g"]
script/source = "extends CharacterBody3D

@export var jump_force = 10.0
@export var gravity = 10
@export var walking_speed = 5
@export var sensitivity = 0.01

func _ready():
	# beginning of game, capture mouse
	Input.mouse_mode = Input.MOUSE_MODE_CAPTURED

func _physics_process(delta: float) -> void:
	var move_input = Input.get_vector(\"left\",\"right\",\"back\",\"forward\")
	
	# rotate movement input towards where the camera is looking
	var move_dir = move_input.rotated(%spring.rotation.y)
	
	# apply walking speed
	move_dir *= walking_speed
	
	# apply movement
	velocity.x = -move_dir.x
	velocity.z = move_dir.y
	
	# apply gravity
	velocity.y -= gravity * delta
	
	# movement_2d = movement without up/down
	var movement_2d = Vector3(velocity.x, 0, velocity.z)
	# rotate possum model towards where we're walking
	if(movement_2d.length() > 1.0):
		%Possum.look_at(transform.origin - movement_2d)

	move_and_slide()

func _input(event: InputEvent):
	if(event.is_action_pressed(\"jump\") and is_on_floor()):
		print(\"jump!\")
		velocity.y = jump_force
	if(event is InputEventMouseMotion):
		# apply mouse rotation
		%spring.rotation.x += event.relative.y * sensitivity
		%spring.rotation.y -= event.relative.x * sensitivity
		# limit vertical rotation 
		%spring.rotation_degrees.x = clampf(%spring.rotation_degrees.x,-45,45)
"

[sub_resource type="CapsuleShape3D" id="CapsuleShape3D_rkbax"]
height = 2.2841797

[node name="Player" type="CharacterBody3D"]
script = SubResource("GDScript_oul6g")

[node name="Possum" type="MeshInstance3D" parent="."]
unique_name_in_owner = true
mesh = ExtResource("1_oul6g")

[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 1.1420898, 0)
shape = SubResource("CapsuleShape3D_rkbax")

[node name="spring" type="SpringArm3D" parent="."]
unique_name_in_owner = true
transform = Transform3D(0.99999994, 0, 0, 0, 0.99999994, 0, 0, 0, 1.0000002, 0, 1.9530652, 0)
spring_length = -5.0

[node name="Camera3D" type="Camera3D" parent="spring"]
transform = Transform3D(-1.0000001, 0, 8.7422784e-08, 0, 1.0000001, 0, -8.742277e-08, 0, -0.9999999, 0, 0, 0)
current = true
